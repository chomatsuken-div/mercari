.devise
  = render "devise/shared/header"
  .deviseSection
    .inner.inner--narrow
      .title.title--narrow
        %p.newText アカウントをお持ちでない方はこちら
        = link_to"新規会員登録", new_user_path, class: "deviseBtn--skyblue"
        .deviseBtn--blue.btn--sns
          = link_to "Facebookでログイン",user_facebook_omniauth_authorize_path
          .sns--i
            = fa_icon 'facebook'
        = link_to "Googleでログイン",user_google_oauth2_omniauth_authorize_path, class: "deviseBtn--white btn--sns"
      .deviseForm.deviseForm--none
        = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
          .deviseForm__field
            = f.email_field :email, autofocus: true, autocomplete: "email",placeholder: "メールアドレス", class: "inputField", required: true
          .deviseForm__field
            = f.password_field :password, autocomplete: "off", placeholder: "パスワード", class: "inputField", required: true
          - if devise_mapping.rememberable?
            .remember__field
              = f.check_box :remember_me
              = f.label :このパスワードを記憶する
          .deviseForm__field
            = recaptcha_tags
          .deviseForm__actions
            = f.submit "ログイン", class: "deviseBtn"
        = link_to"パスワードをお忘れの方", new_user_password_path
  = render "devise/shared/footer"
